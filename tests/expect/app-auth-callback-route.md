# Expect flow: app/auth/callback/route.ts

Open the route represented by app/auth/callback/route.ts. Exercise the primary user-visible workflow, resize to mobile, check for console errors, and verify the page completes the feature described in the active plan.
