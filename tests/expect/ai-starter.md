# Expect flow: app/ai-starter/page.tsx

Open `/ai-starter` from the installed app.

Verify:
- The starter hub renders without console errors.
- The module/status cards explain install, hooks, research, verification, browser proof, Expect, supervisor, design registry, and control plane coverage.
- The page remains readable on mobile width.
- Links or controls do not expose raw secrets or broken JSON.
