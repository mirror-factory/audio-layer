# Meeting notes-package API companion alias

This file exists for the Codex post-tool companion hook, whose glob treats the
dynamic `[id]` route segment as a character class. The full API contract lives
in `docs/apis/app-api-meetings-id-notes-package-route.md`.

Covered surface: `app/api/meetings/[id]/notes-package/route.ts`.
